name: Go Tests

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    environment: live
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        path: todo-vibe
        
    - name: Checkout vibes-framework
      uses: actions/checkout@v3
      with:
        repository: amoscookeh/go-vibes
        path: vibes-framework
      
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'
        
    - name: Run tests
      id: run_tests
      run: |
        cd todo-vibe
        go mod tidy
        # Run tests and capture output
        go test -v ./... > test_output.txt 2>&1 || true
        # Display output
        cat test_output.txt
        # Check for failures and exit appropriately
        if grep -q "FAIL" test_output.txt; then
          exit 1
        fi

    - name: Get Gemini suggestions for failing tests
      if: failure()
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        PINECONE_API_KEY: ${{ secrets.PINECONE_API_KEY }}
      run: |
        cd todo-vibe
        
        # Move test failures to scripts directory
        mv test_output.txt scripts/
        
        # Run the analyzer script - make sure it has correct dependencies and files
        cd scripts
        go mod tidy
        go run ai_test_analyzer.go
        echo "🔍 See Gemini analysis in the job logs" 
